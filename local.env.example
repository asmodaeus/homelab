# Lokale Konfiguration – Kopie als "local.env" anlegen (wird von .gitignore ignoriert)
# Verwendung:
#   cp local.env.example local.env
#   vim local.env
#   ./dev/bootstrap-local.sh

# NAS: IP-Adresse und NFS-Export-Pfad
NAS_IP=192.168.1.x
NAS_PATH=/volume1/path
