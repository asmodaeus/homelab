---
- name: Restart chrony
  ansible.builtin.systemd:
    name: chrony
    state: restarted

- name: Restart sshd
  ansible.builtin.systemd:
    name: sshd
    state: restarted

- name: Set reboot required
  ansible.builtin.set_fact:
    cgroup_config_changed: true
